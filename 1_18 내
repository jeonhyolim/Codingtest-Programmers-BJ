def solution(a, b):
    answer = 0
    for i in range(len(a)):
        answer+=a[i]*b[i]
    return answer

# zip 이용해서 풀기
def solution(a, b):
    return sum([x*y for x, y in zip(a,b)])

# lambda와 zip 쓰기
solution = lambda x, y: sum(a*b for a, b in zip(x, y))
